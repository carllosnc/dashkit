# Textarea

A multiline text input component with support for labels, error states, and auto-growing height.

## Import

```tsx
import { Textarea } from '@/components/Textarea/Textarea'
```

## Installation

Add the textarea component to your project using the Dashkit CLI:

```bash
bunx carllosnc/dashkit add textarea
```

## Basic Usage

The basic usage of the textarea with a label and placeholder.

<Preview>
  <Textarea label="Biography" placeholder="Tell us about yourself..." />
</Preview>

```tsx
<Textarea
  label="Biography"
  placeholder="Tell us about yourself..."
/>
```

## Auto-Grow

Use the `autoGrow` prop to allow the textarea to automatically adjust its height based on the content. This provides a better user experience for long-form content.

<Preview>
  <div className="w-full">
    <Textarea
      label="Message"
      placeholder="Type something long to see it grow..."
      autoGrow
    />
  </div>
</Preview>

```tsx
<Textarea
  label="Message"
  placeholder="Type away..."
  autoGrow
/>
```

## States

The textarea supports error and disabled states, just like the input component.

<Preview>
  <div className="flex flex-col gap-6 w-full">
    <Textarea
      label="Notes"
      error="Something went wrong"
      defaultValue="Invalid input content..."
    />
    <Textarea
      label="Locked Field"
      disabled
      value="This content is read-only."
      helperText="You cannot edit this information"
    />
  </div>
</Preview>

```tsx
<Textarea
  label="Notes"
  error="Something went wrong"
  defaultValue="Invalid content..."
/>

<Textarea
  label="Locked Field"
  disabled
  value="Read-only text."
  helperText="You cannot edit this"
/>
```

## Props

The component extends all native `TextareaHTMLAttributes<HTMLTextAreaElement>` props, plus the following:


<PropertyDoc
  properties={[
  {
    "prop": "label",
    "type": "string",
    "defaultValue": "—",
    "description": "Textarea label rendered above the field."
  },
  {
    "prop": "autoGrow",
    "type": "boolean",
    "defaultValue": "false",
    "description": "Whether the textarea should automatically adjust its height."
  },
  {
    "prop": "error",
    "type": "string",
    "defaultValue": "—",
    "description": "Error message displayed below the field."
  },
  {
    "prop": "helperText",
    "type": "string",
    "defaultValue": "—",
    "description": "Helper message displayed below the field."
  },
  {
    "prop": "className",
    "type": "string",
    "defaultValue": "—",
    "description": "Additional CSS classes for the textarea element."
  },
  {
    "prop": "ref",
    "type": "Ref<HTMLTextAreaElement>",
    "defaultValue": "—",
    "description": "Forwarded ref to the underlying <textarea> element."
  }
]}
/>




