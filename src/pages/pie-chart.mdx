import { PieChart } from '../components/PieChart/PieChart';
import { Card } from '../components/Card/Card';

# Pie Chart

The **PieChart** component is an expressive tool for visualizing categorical distributions. It features interactive segments with tooltips, smooth hover scaling, and supports donut variations via the `innerRadius` prop.

## Examples

### Classic Pie Chart

<Card className='not-prose'>
<PieChart
  data={[
    { label: 'Chrome', value: 400, color: 'var(--color-blue-500)' },
    { label: 'Firefox', value: 300, color: 'var(--color-orange-500)' },
    { label: 'Safari', value: 200, color: 'var(--color-sky-400)' },
    { label: 'Edge', value: 100, color: 'var(--color-emerald-500)' },
  ]}
/>
</Card>

```tsx
import { PieChart } from 'dashkit-ui';

function Example() {
  const data = [
    { label: 'Direct', value: 400, color: '#0ea5e9' },
    { label: 'Social', value: 300, color: '#f59e0b' },
    //...
  ];

  return <PieChart data={data} />;
}
```

### Donut Chart

Transform any pie chart into a donut chart by setting the `innerRadius` property (0 to 1).

<Card className='not-prose'>
<PieChart 
  innerRadius={0.6}
  data={[
    { label: 'Active', value: 45, color: '#10b981' },
    { label: 'Inactive', value: 52, color: '#64748b' },
    { label: 'Pending', value: 48, color: '#3b82f6' },
  ]}
/>
</Card>

```tsx
<PieChart 
  innerRadius={0.6} 
  data={userData} 
/>
```

## API Reference

### PieChart


<PropertyDoc
  properties={[
  {
    "prop": "data",
    "type": "PieChartDataItem[]",
    "defaultValue": "—",
    "description": "Array of objects with label, value, and color."
  },
  {
    "prop": "innerRadius",
    "type": "number",
    "defaultValue": "0",
    "description": "Radius of the center hole (0 for Pie, > 0 for Donut)."
  },
  {
    "prop": "showLabels",
    "type": "boolean",
    "defaultValue": "true",
    "description": "Display the legend below the chart."
  },
  {
    "prop": "showTooltip",
    "type": "boolean",
    "defaultValue": "true",
    "description": "Enable interactive segment tooltips."
  },
  {
    "prop": "animate",
    "type": "boolean",
    "defaultValue": "true",
    "description": "Enable entrance animations."
  },
  {
    "prop": "className",
    "type": "string",
    "defaultValue": "—",
    "description": "Additional CSS for the container."
  }
]}
/>



