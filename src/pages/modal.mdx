import { useState } from 'react';
import { Modal, ModalHeader, ModalContent, ModalFooter } from '../dashkit/Modal/Modal';
import { Button } from '../dashkit/Button/Button';
import { FiLayers, FiDownload, FiTrash2, FiAlertTriangle, FiCheckCircle } from 'react-icons/fi';

export const BasicModal = () => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
      <Button onClick={() => setIsOpen(true)}>Open Modal</Button>
      <Modal isOpen={isOpen} onClose={() => setIsOpen(false)}>
        <ModalHeader onClose={() => setIsOpen(false)}>
          <h2 className="text-base font-medium tracking-tight text-block-fg dark:text-block-dark-fg">Project Overview</h2>
          <p className="text-sm text-ds-500">Review your latest project stats and activity log.</p>
        </ModalHeader>
        <ModalContent>
          <p className="text-sm py-2">
            This basic implementation demonstrates the simplified composition API.
            Modals now require explicit structure for header, content, and footer areas.
          </p>
        </ModalContent>
        <ModalFooter>
          <Button variant="outlined" onClick={() => setIsOpen(false)}>Cancel</Button>
          <Button onClick={() => setIsOpen(false)}>Save Changes</Button>
        </ModalFooter>
      </Modal>
    </>
  );
};

export const ComposableModal = () => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
      <Button variant="outlined" onClick={() => setIsOpen(true)}>Manage Team</Button>
      <Modal isOpen={isOpen} onClose={() => setIsOpen(false)} size="lg">
        <ModalHeader onClose={() => setIsOpen(false)}>
          <h2 className="text-base font-medium tracking-tight text-block-fg dark:text-block-dark-fg">Team Settings</h2>
          <p className="text-sm text-ds-500">Configure access levels and roles for your workspace.</p>
        </ModalHeader>
        <ModalContent>
          <div className="flex flex-col gap-4 py-4">
            <div className="p-4 ds-rounded bg-ds-100/50 dark:bg-ds-800 border border-ds-200 dark:border-ds-700">
              <span className="text-xs font-bold uppercase text-ds-500">Owner</span>
              <p className="text-sm font-semibold">carllos@example.com</p>
            </div>
            <div className="p-4 ds-rounded border border-dashed border-ds-300 dark:border-ds-700 hover:bg-ds-100/30 dark:hover:bg-ds-800/30 transition-colors cursor-pointer group">
              <p className="text-sm text-ds-500 font-medium text-center group-hover:text-ds-900 dark:group-hover:text-white">+ Invite new member</p>
            </div>
          </div>
        </ModalContent>
        <ModalFooter>
          <Button variant="outlined" onClick={() => setIsOpen(false)}>Cancel</Button>
          <Button onClick={() => setIsOpen(false)}>Save Changes</Button>
        </ModalFooter>
      </Modal>
    </>
  );
};

export const AlertModalExample = () => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
      <Button variant="outlined" color="error" leftIcon={<FiAlertTriangle />} onClick={() => setIsOpen(true)}>Delete Workspace</Button>
      <Modal isOpen={isOpen} onClose={() => setIsOpen(false)} size="sm" className="text-center">
        <ModalContent>
          <div className="flex flex-col items-center gap-6 py-6">
            <div className="size-20 rounded-full bg-red-50 dark:bg-red-500/10 flex items-center justify-center text-red-600 dark:text-red-400 border border-red-100 dark:border-red-500/20">
              <FiTrash2 size={40} />
            </div>
            <div className="flex flex-col gap-2">
              <h3 className="text-xl font-bold text-ds-900 dark:text-white tracking-tight">Delete Workspace?</h3>
              <p className="text-sm text-ds-500 max-w-[240px]">This action is permanent and will remove all project data associated with this workspace.</p>
            </div>
            <div className="grid grid-cols-2 w-full gap-2 pt-4">
              <Button variant="outlined" onClick={() => setIsOpen(false)}>
                Cancel
              </Button>
              <Button variant="filled" onClick={() => setIsOpen(false)}>
                Confirm Delete
              </Button>
            </div>
          </div>
        </ModalContent>
      </Modal>
    </>
  );
};

export const FullScreenExample = () => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
      <Button variant="outlined" leftIcon={<FiLayers />} onClick={() => setIsOpen(true)}>Immersive Editor</Button>
      <Modal isOpen={isOpen} onClose={() => setIsOpen(false)} size="full">
        <ModalHeader onClose={() => setIsOpen(false)}>
          <h2 className="text-base font-medium tracking-tight text-block-fg dark:text-block-dark-fg">Immersive Editor</h2>
          <p className="text-sm text-ds-500">Maximize your creative potential in our distilled editing mode.</p>
        </ModalHeader>
        <ModalContent className="h-full flex items-center justify-center pt-0">
          <div>
            <div className="flex flex-col items-center gap-4 text-center">
              <div className="size-12 rounded-xl bg-ds-100 dark:bg-ds-900 flex items-center justify-center text-ds-400">
                <FiCheckCircle size={24} />
              </div>
              <h2 className="text-2xl font-bold text-ds-900 dark:text-white tracking-tight leading-[1.05]">Workspace Ready</h2>
              <p className="text-ds-500 max-w-sm mx-auto">Click X to return to your dashboard or begin customizing your layout.</p>
            </div>
          </div>
        </ModalContent>
        <ModalFooter>
          <Button variant="outlined" onClick={() => setIsOpen(false)}>Exit Immersive Mode</Button>
          <Button onClick={() => setIsOpen(false)}>Launch Editor</Button>
        </ModalFooter>
      </Modal>
    </>
  );
};


# Modal

A centered, layered dialog component that focuses user attention on critical information or actions.

## Import

```tsx
import { Modal, ModalHeader, ModalContent, ModalFooter } from '@/components/Modal/Modal'
```

## Installation

Add the modal to your project using the Dashkit CLI:

```bash
bunx carllosnc/dashkit add modal
```

## Usage

The modal follows a strict **Composition-based API** for maximum flexibility and layout control.

### Basic Implementation

<Preview>
  <div className="flex flex-wrap gap-4">
    <BasicModal />
  </div>
</Preview>

```tsx
<Modal isOpen={isOpen} onClose={close}>
  <ModalHeader onClose={close}>
    <h2>Project Overview</h2>
    <p>Review your latest project stats.</p>
  </ModalHeader>
  <ModalContent>
    <p>Main content area...</p>
  </ModalContent>
  <ModalFooter>
    <Button variant="outlined" onClick={close}>Cancel</Button>
    <Button onClick={save}>Save Changes</Button>
  </ModalFooter>
</Modal>
```

### Advanced Layouts

Use the footer for standardized action alignment.

<Preview>
  <div className="flex flex-wrap gap-4">
    <ComposableModal />
  </div>
</Preview>

```tsx
<Modal isOpen={isOpen} onClose={close} size="lg">
  <ModalHeader onClose={close}>
    <h2>Team Management</h2>
    <p>Invite and manage roles.</p>
  </ModalHeader>
  <ModalContent>
    <div className="team-list">
      {/* Team settings content */}
    </div>
  </ModalContent>
  <ModalFooter>
    <Button variant="outlined" onClick={close}>Cancel</Button>
    <Button onClick={save}>Save Changes</Button>
  </ModalFooter>
</Modal>
```

## Formats & Examples

### Alert Modal

Smaller modals with centered content for destructive actions.

<Preview>
  <AlertModalExample />
</Preview>

```tsx
<Modal isOpen={isOpen} size="sm" className="text-center">
  <ModalContent>
    <div className="flex flex-col items-center gap-6">
      <FiTrash2 size={40} className="text-red-500" />
      <h3>Delete workspace?</h3>
      <div className="grid grid-cols-2 w-full gap-2">
        <Button variant="outlined">Cancel</Button>
        <Button variant="filled">Confirm Delete</Button>
      </div>
    </div>
  </ModalContent>
</Modal>
```

### Full-Screen (Immersive)

The `full` size provides a take-over experience while maintaining focus on a single task.

<Preview>
  <FullScreenExample />
</Preview>

```tsx
<Modal isOpen={isOpen} size="full">
  <ModalHeader onClose={close}>
    <h2>Immersive Editor</h2>
  </ModalHeader>
  <ModalContent className="h-full">
     <div className="h-full border-2 border-dashed rounded-[2.5rem]">
       {/* Editor area content */}
     </div>
  </ModalContent>
  <ModalFooter>
    <Button variant="outlined" onClick={close}>Exit</Button>
    <Button onClick={launch}>Launch</Button>
  </ModalFooter>
</Modal>
```

## Features

- **Spring-Based Animations**: Uses `framer-motion` for a premium scale/fade entry.
- **Backdrop Blur**: Softly blurs background content for better focus.
- **Body Scroll Lock**: Automatically prevents scrolling when the modal is active.
- **Portalled Rendering**: Renders at the document root to avoid layering issues.
- **Keyboard Accessible**: Closes instantly when the `Escape` key is pressed.

## Props (API)

<PropertyDoc
  properties={[
  {
    "prop": "isOpen",
    "type": "boolean",
    "defaultValue": "—",
    "description": "Controls if the modal is shown."
  },
  {
    "prop": "onClose",
    "type": "() => void",
    "defaultValue": "—",
    "description": "Callback fired when closing (ESC or click outside)."
  },
  {
    "prop": "size",
    "type": "'sm' | 'md' | 'lg' | 'xl' | '2xl' | 'full'",
    "defaultValue": "'md'",
    "description": "The max-width of the internal container."
  },
  {
    "prop": "children",
    "type": "ReactNode",
    "defaultValue": "—",
    "description": "Composition sub-components (ModalHeader, ModalContent, ModalFooter)."
  },
  {
    "prop": "className",
    "type": "string",
    "defaultValue": "—",
    "description": "Custom classes for the modal container."
  }
]}
/>

