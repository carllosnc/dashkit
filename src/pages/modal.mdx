# Modal

A centered, layered dialog component that focuses user attention on critical information or actions.

## Import

```tsx
import { Modal } from '@/components/Modal/Modal'
```

## Installation

Add the modal to your project using the Dashkit CLI:

```bash
bunx carllosnc/dashkit add modal
```

## Basic Usage

The modal is a controlled component toggled by the `isOpen` prop.

<Preview>
  <ModalDemo />
</Preview>

```tsx
import { useState } from 'react'
import { Modal } from '@/components/Modal/Modal'

export default function MyPage() {
  const [isOpen, setIsOpen] = useState(false)

  return (
    <>
      <button onClick={() => setIsOpen(true)}>Open Modal</button>
      
      <Modal
        isOpen={isOpen}
        onClose={() => setIsOpen(false)}
        title="Delete Item"
        description="Are you sure you want to proceed?"
      >
        <p>This action cannot be undone.</p>
      </Modal>
    </>
  )
}
```

## Features

- **Spring-Based Animations**: Uses `framer-motion` for a premium scale/fade entry.
- **Backdrop Blur**: Softly blurs background content for better focus.
- **Body Scroll Lock**: Automatically prevents scrolling when the modal is active.
- **Portalled Rendering**: Renders at the document root to avoid z-index layering issues.
- **Keyboard Accessible**: Closes instantly when the `Escape` key is pressed.

## Sizes

The component includes predefined sizes to accommodate different content types.

- `sm`: Max width 384px (Task alerts)
- `md` (Default): Max width 448px (Standard forms)
- `lg`: Max width 512px
- `xl`: Max width 576px
- `2xl`: Max width 672px
- `full`: Immersive, full-screen takeover with rounded corners.

## Props

The `Modal` component accepts the following props:


<PropertyDoc
  properties={[
  {
    "prop": "isOpen",
    "type": "boolean",
    "defaultValue": "—",
    "description": "Controls if the modal is shown."
  },
  {
    "prop": "onClose",
    "type": "() => void",
    "defaultValue": "—",
    "description": "Callback fired when closing (ESC or click outside)."
  },
  {
    "prop": "title",
    "type": "string",
    "defaultValue": "—",
    "description": "Bold header title."
  },
  {
    "prop": "description",
    "type": "string",
    "defaultValue": "—",
    "description": "Subtitle text below the title."
  },
  {
    "prop": "size",
    "type": "'sm' | 'md' | 'lg' | 'xl' | '2xl' | 'full'",
    "defaultValue": "'md'",
    "description": "The max-width of the internal container."
  },
  {
    "prop": "footer",
    "type": "ReactNode",
    "defaultValue": "—",
    "description": "Actions displayed at the bottom of the modal."
  },
  {
    "prop": "children",
    "type": "ReactNode",
    "defaultValue": "—",
    "description": "Main content of the dialog."
  },
  {
    "prop": "className",
    "type": "string",
    "defaultValue": "—",
    "description": "Custom classes for the modal container."
  },
  {
    "prop": "showCloseButton",
    "type": "boolean",
    "defaultValue": "true",
    "description": "Whether to show the X icon in headers."
  }
]}
/>



