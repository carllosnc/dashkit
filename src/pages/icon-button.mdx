# Icon Button

A premium, specialized button for icon-only actions with customizable variant and rounding options.

## Installation

```bash
bunx carllosnc/dashkit add icon-button
```

## Usage

```tsx
import { IconButton } from '@/components/dashkit';
import { FiEdit, FiTrash, FiShare } from 'react-icons/fi';

export default function Actions() {
  return (
    <div className="flex gap-4">
      <IconButton icon={<FiEdit />} variant="soft" />
      <IconButton icon={<FiShare />} variant="ghost" />
      <IconButton icon={<FiTrash />} variant="filled" rounded />
    </div>
  );
}
```

## Overview

The `IconButton` component provides a consistent container for circular or rounded square icon triggers. It's often used in toolbars, navigation bars, and as utility shortcuts throughout application layouts.

<Preview>
  <IconButtonDemo />
</Preview>

## Props


<PropertyDoc
  properties={[
  {
    "prop": "icon",
    "type": "ReactNode",
    "defaultValue": "-",
    "description": "The icon component to render inside the button."
  },
  {
    "prop": "variant",
    "type": "'filled' | 'soft' | 'ghost'",
    "defaultValue": "'soft'",
    "description": "Visual style variant of the button."
  },
  {
    "prop": "rounded",
    "type": "boolean",
    "defaultValue": "false",
    "description": "If true, applies rounded-full, otherwise rounded-md."
  },
  {
    "prop": "disabled",
    "type": "boolean",
    "defaultValue": "false",
    "description": "Whether the button is interactive."
  }
]}
/>



