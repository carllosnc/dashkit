import { useState } from 'react';
import { 
  Sidebar, 
  SidebarHeader, 
  SidebarHeaderOpen,
  SidebarHeaderClose,
  SidebarFooter, 
  SidebarFooterOpen,
  SidebarFooterClose,
  SidebarSection, 
  SidebarItem 
} from '../components/Sidebar/Sidebar';
import { Badge } from '../components/Badge';
import { 
  FiLayout, 
  FiUsers, 
  FiSettings, 
  FiActivity, 
  FiPieChart, 
  FiShield, 
  FiLifeBuoy,
  FiUser,
  FiCalendar,
  FiDatabase,
  FiServer,
  FiLock,
  FiMessageSquare,
  FiLayers,
  FiBriefcase
} from 'react-icons/fi';

export const InteractiveSidebar = () => {
  const [activeTab, setActiveTab] = useState('dashboard');
  const [isOpen, setIsOpen] = useState(true);

  return (
    <div className="flex h-[450px] w-full border border-ds-200 dark:border-ds-800 rounded-2xl overflow-hidden bg-white dark:bg-ds-950 shadow-sm relative">
      <Sidebar
        collapsible
        open={isOpen}
        onOpenChange={setIsOpen}
      >
        <SidebarHeader>
          <SidebarHeaderOpen>
            <div className="flex items-center gap-3 px-2">
              <div className="size-8 rounded-lg bg-ds-900 dark:bg-white flex items-center justify-center">
                <FiLayers className="size-4 text-white dark:text-ds-900" />
              </div>
              <span className="font-bold text-base tracking-tight text-ds-950 dark:text-white">Dashkit</span>
            </div>
          </SidebarHeaderOpen>
          <SidebarHeaderClose>
            <div className="size-8 rounded-lg bg-ds-900 dark:bg-white flex items-center justify-center mx-auto">
              <FiLayers className="size-4 text-white dark:text-ds-900" />
            </div>
          </SidebarHeaderClose>
        </SidebarHeader>

        <SidebarSection title="General" collapsible>
          <SidebarItem icon={<FiLayout size={18} />} active={activeTab === 'dashboard'} onClick={() => setActiveTab('dashboard')}>
            Overview
          </SidebarItem>
          <SidebarItem icon={<FiActivity size={18} />} active={activeTab === 'analytics'} onClick={() => setActiveTab('analytics')}>
            Analytics
          </SidebarItem>
        </SidebarSection>

        <SidebarSection title="Management" collapsible badge={<Badge content="2" variant="soft" color="info" />}>
          <SidebarItem icon={<FiUsers size={18} />} active={activeTab === 'team'} onClick={() => setActiveTab('team')}>
            Team
          </SidebarItem>
          <SidebarItem icon={<FiSettings size={18} />} active={activeTab === 'settings'} onClick={() => setActiveTab('settings')}>
            Settings
          </SidebarItem>
        </SidebarSection>

        <SidebarFooter className="p-4 border-t border-ds-100 dark:border-ds-900">
          <div className="flex items-center gap-3 px-2 py-2 rounded-xl group hover:bg-ds-50 dark:hover:bg-ds-800 transition-all cursor-pointer">
            <div className="size-8 rounded-full bg-ds-200 dark:bg-ds-700 flex items-center justify-center overflow-hidden">
              <FiUser size={16} />
            </div>
            <SidebarHeaderOpen>
              <div className="flex-1 overflow-hidden text-left">
                <p className="text-xs font-bold truncate text-ds-950 dark:text-white">John Doe</p>
                <p className="text-[10px] text-ds-500 truncate">Admin</p>
              </div>
            </SidebarHeaderOpen>
          </div>
        </SidebarFooter>
      </Sidebar>
      <div className="flex-1 bg-ds-50/50 dark:bg-ds-900/20 p-8 flex items-center justify-center">
        <p className="text-ds-400 text-sm font-medium italic">Active: {activeTab}</p>
      </div>
    </div>
  );
};

export const CollapsibleSectionExample = () => {
  return (
    <div className="flex h-[400px] w-full border border-ds-200 dark:border-ds-800 rounded-2xl overflow-hidden bg-white dark:bg-ds-950 shadow-sm relative">
      <Sidebar>
        <SidebarHeader>
          <SidebarHeaderOpen>
            <div className="font-bold px-6">Sections</div>
          </SidebarHeaderOpen>
          <SidebarHeaderClose>
            <div className="size-8 rounded-full bg-ds-200 dark:bg-ds-800 flex items-center justify-center overflow-hidden">
              <FiUser size={18} />
            </div>
          </SidebarHeaderClose>
        </SidebarHeader>
        <div className="flex-1 overflow-y-auto">
          <SidebarSection title="Account" collapsible>
            <SidebarItem icon={<FiUser size={18} />}>Profile</SidebarItem>
            <SidebarItem icon={<FiShield size={18} />}>Security</SidebarItem>
          </SidebarSection>
          <SidebarSection title="Settings" collapsible defaultOpen={false}>
            <SidebarItem icon={<FiSettings size={18} />}>Basic Settings</SidebarItem>
            <SidebarItem icon={<FiActivity size={18} />}>Logs</SidebarItem>
          </SidebarSection>
        </div>
      </Sidebar>
      <div className="flex-1 bg-ds-50/50 dark:bg-ds-900/20 p-8 flex items-center justify-center text-ds-400 text-sm">
        Documentation Preview
      </div>
    </div>
  );
};

# Sidebar

A versatile and premium navigation sidebar component that supports complex layouts, branding, and status indicators using a flexible composition pattern.

## Import

```tsx
import { 
  SidebarHeader, 
  SidebarHeaderOpen,
  SidebarHeaderClose,
  SidebarFooter, 
  SidebarFooterOpen,
  SidebarFooterClose,
  SidebarSection, 
  SidebarItem
} from '@/components/Sidebar/Sidebar'
```

## Installation

Add the sidebar into your project using the Dashkit CLI:

```bash
bunx carllosnc/dashkit add sidebar
```

## Basic Usage

The `Sidebar` uses a semantic composition of headers, sections, and footers to create a cohesive navigation experience.

<div className="not-prose">
<InteractiveSidebar />
</div>

```tsx
import { Sidebar, SidebarHeader, SidebarSection, SidebarItem } from '@/components/dashkit';
import { FiLayout, FiActivity, FiSettings } from 'react-icons/fi';

export default function App() {
  const [activeTab, setActiveTab] = useState('dashboard');

  return (
    <Sidebar className="w-64 border-r">
      <SidebarHeader>
        <Logo />
        <span>Dashkit</span>
      </SidebarHeader>

      <SidebarSection title="General">
        <SidebarItem
          icon={<FiLayout />}
          active={activeTab === 'dashboard'}
          onClick={() => setActiveTab('dashboard')}
        >
          Overview
        </SidebarItem>
      </SidebarSection>
    </Sidebar>
  );
}
```

## Components Breakdown

The Sidebar library is composed of several specialized layers designed for specific layout roles:

<PropertyDoc
  properties={[
    { "prop": "Sidebar", "description": "The main container that handles height, width, and transitions." },
    { "prop": "SidebarHeader", "description": "Designed for branding, logos, or organization switchers." },
    { "prop": "SidebarHeaderOpen/Close", "description": "Conditional wrappers that show content based on sidebar expansion." },
    { "prop": "SidebarFooter", "description": "Bottom section for user profiles, help links, or versioning." },
    { "prop": "SidebarFooterOpen/Close", "description": "Conditional wrappers for specialized footer layouts." },
    { "prop": "SidebarSection", "description": "Groups related items with an optional title." },
    { "prop": "SidebarItem", "description": "Individual navigation links or interactive buttons." }
  ]}
/>

## Collapsible Sidebar

Enable the `collapsible` prop to show a floating toggle button on the sidebar edge. You can use the `isOpen` state to conditionally render content using native utility components.

```tsx
<Sidebar collapsible defaultOpen={true}>
  <SidebarHeader>
    <SidebarHeaderOpen>Full Branding</SidebarHeaderOpen>
    <SidebarHeaderClose>Logo</SidebarHeaderClose>
  </SidebarHeader>
  {/* ... items ... */}
</Sidebar>
```

## Collapsible Sections

Sections can be individually collapsed to conserve vertical space or group related functionality. Use the `collapsible` prop on `SidebarSection` to enable this behavior. You can also control the initial expansion state with the `defaultOpen` prop.

<Preview>
  <CollapsibleSectionExample />
</Preview>

```tsx
<SidebarSection title="Account" collapsible>
  <SidebarItem icon={<FiUser />}>Profile</SidebarItem>
  <SidebarItem icon={<FiShield />}>Security</SidebarItem>
</SidebarSection>

<SidebarSection title="Settings" collapsible defaultOpen={false}>
  <SidebarItem icon={<FiSettings />}>Advanced</SidebarItem>
</SidebarSection>
```

## API Reference

### Sidebar

<PropertyDoc
  properties={[
    { "prop": "children", "type": "ReactNode", "defaultValue": "—", "description": "The content of the sidebar." },
    { "prop": "collapsible", "type": "boolean", "defaultValue": "true", "description": "Shows a floating toggle button on the edge." },
    { "prop": "defaultOpen", "type": "boolean", "defaultValue": "true", "description": "Initial state for uncontrolled sidebar." },
    { "prop": "open", "type": "boolean", "defaultValue": "—", "description": "Controlled open state." },
    { "prop": "onOpenChange", "type": "(open: boolean) => void", "defaultValue": "—", "description": "Event handler for state changes." },
    { "prop": "className", "type": "string", "defaultValue": "—", "description": "Custom classes for the container." }
  ]}
/>

### SidebarSection

<PropertyDoc
  properties={[
    { "prop": "title", "type": "string", "defaultValue": "—", "description": "Uppercase header above the section." },
    { "prop": "badge", "type": "ReactNode", "defaultValue": "—", "description": "Status badge displayed in the section header." },
    { "prop": "children", "type": "ReactNode", "defaultValue": "—", "description": "SidebarItems to be rendered." },
    { "prop": "collapsible", "type": "boolean", "defaultValue": "false", "description": "Whether the section can be collapsed." },
    { "prop": "defaultOpen", "type": "boolean", "defaultValue": "true", "description": "Initial expansion state for the section." }
  ]}
/>

### SidebarItem

<PropertyDoc
  properties={[
    { "prop": "active", "type": "boolean", "defaultValue": "false", "description": "Applies active highlight styling." },
    { "prop": "icon", "type": "ReactNode", "defaultValue": "—", "description": "Icon to be displayed beside the text." },
    { "prop": "children", "type": "ReactNode", "defaultValue": "—", "description": "Descriptive text for the nav link." },
    { "prop": "onClick", "type": "() => void", "defaultValue": "—", "description": "Standard click event handler." }
  ]}
/>
